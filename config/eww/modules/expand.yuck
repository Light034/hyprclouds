;; Variable to control the expand state
(defvar show_picker false)

;; Expand button widget with tools
(defwidget expand []
 (eventbox :onhover "eww update show_picker=true"
          :onhoverlost "eww update show_picker=false"
 (box :orientation "horizontal"
     :space-evenly false
     (button :class "expand"
             (label :text ""))
     (revealer :transition "slideleft"
              :reveal show_picker
              (box :orientation "horizontal"
                   :space-evenly false
                   :class "tools-box"
                   (button :class "picker"
                   ;; ToDo: Update directory for scripts and make unify them. Also to update that the path doesn't brake upon changing other configs.
                          :onclick "sleep 0.1 && scripts/picker.sh &"
                          (label :text "󰃉"))
                   (button :class "screenshot"
                          :onclick "sleep 0.1 && scripts/screenshot &"
                          (label :text "")))))))
                          